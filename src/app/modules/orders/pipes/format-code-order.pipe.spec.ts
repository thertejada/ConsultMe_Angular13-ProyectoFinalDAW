import { FormatCodeOrderPipe } from './format-code-order.pipe';

describe('FormatCodeOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCodeOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
